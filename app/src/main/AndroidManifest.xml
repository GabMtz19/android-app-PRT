<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.itesm.testbasicapi">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestingProductsAPI"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".controller.activity.Forms"
            android:exported="true" />
        <activity
            android:name=".controller.activity.OnBoarding"
            android:exported="true" />
        <activity
            android:name=".controller.activity.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".controller.activity.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.activity.MainActivity"
            android:exported="true" />
    </application>

</manifest>